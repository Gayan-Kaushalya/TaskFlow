plugin: amazon.aws.aws_ec2
regions:
  - us-east-1

filters:
  instance-state-name:
    - running
  "tag:Name":
    - taskflow-ecs-node

hostnames:
  - instance-id

compose:
  ansible_host: instance_id
