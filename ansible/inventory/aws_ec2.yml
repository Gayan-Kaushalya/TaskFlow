plugin: amazon.aws.aws_ec2
regions:
  - us-east-1

filters:
  instance-state-name:
    - running
  "tag:Name":
    - taskflow-ecs-node

keyed_groups:
  - key: tags.Name
    prefix: tag

hostnames:
  - private-ip-address
  - ip-address
  - dns-name
